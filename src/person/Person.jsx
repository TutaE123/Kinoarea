import React from 'react'
import { PersonCard } from './PersonCard'
import { PersonTitle } from './PersonTitle'

export const Person = () => {
  return (
    <section>
    <PersonTitle />
    <PersonCard />
    </section>
  )
}
